server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health, info, metrics, loggers,  env, heapdump, httptrace, mappings

users:
  server:
    url: http://localhost:8081

products:
  server:
    url: http://localhost:8082

orders:
  server:
    url: http://localhost:8083